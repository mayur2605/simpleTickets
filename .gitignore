node_modules/
dist/
.env
.env.*
!.env.example
playwright-report/
test-results/

# Smoke-test screenshot outputs, regenerated by every `npm test` run
prototype/*-preview.png

# Local probe token for the throwaway runtime-check worker
.probe-token
*.probe-token
