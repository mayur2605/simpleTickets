{
  "name": "runtime-check",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Temporary Cloudflare Workers feasibility probe for SimpleTickets. Delete after the experiment.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "tail": "wrangler tail --format pretty"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260916.1",
    "typescript": "6.0.2",
    "wrangler": "4.132.0"
  }
}
